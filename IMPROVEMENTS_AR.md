# تحسين جودة الأسئلة المُولَّدة آلياً

شرح تفصيلي للتعديلات التي أُجريت على النظام لمعالجة تدنّي جودة الأسئلة المُولَّدة من ملف
`pdf/Code-of-Conduct-English_KFC.pdf`.

---

## ١. المشكلة كما ظهرت

كانت المخرجات تحتوي على أسئلة غير صالحة للاستخدام، من أمثلتها:

```
brand restaurant in Asia to ship supplies from ____ .
    The United States / The Us / A Country / France
```

```
What is the applicable policy or provision regarding as an yum.
recognizes its responsibility to the countries where we do?
    International Business / A Global Company / International Commerce / A Business
```

وتنقسم العيوب إلى أربعة أنواع:

| النوع | الوصف |
|---|---|
| جملة ناقصة | نص السؤال ليس جملة كاملة، بل شظية تبدأ من منتصف الكلام |
| صياغة ركيكة | قالب واحد يُحشى بنص عشوائي فينتج كلاماً غير مفهوم |
| بدائل متطابقة | أكثر من خيار صحيح مثل `The United States` و `The Us` |
| بدائل تفضح الإجابة | خيارات فارغة المعنى مثل `A Country`، أو اختلاف في شكل الحروف |

---

## ٢. الأسباب الجذرية

بعد تتبّع المسار كاملاً، تبيّن أن كل الأعراض السابقة ترجع إلى ثلاثة أخطاء أساسية.

### السبب الأول: النص المُدخل لم يكن نصاً سليماً أصلاً

الدالة `pdf2text` كانت تُمرّر ناتج استخراج الـ PDF كما هو إلى مكتبة spaCy. وهذا الناتج ليس
نثراً، بل يحتوي على:

- سطور فهرس المحتويات مع النقاط المتتابعة (`.......5`)
- رؤوس وتذييلات الصفحات المتكررة
- أرقام الصفحات المنفردة
- كلمات مقطوعة بشرطة في نهاية السطر

ثم جاء الخطأ الأكبر: الدالة `clean_text` كانت **تحذف كل علامات الترقيم** ثم تعيد ربط الجمل
بـ `". "`. وبما أن حدود الجمل تعتمد على الترقيم، فقد أدى ذلك إلى:

- انقسام الجملة عند علامة التعجّب في الاسم التجاري `Yum!`، فصارت
  `"A Yum! brand restaurant in Asia..."` تُقرأ كشظية
  `"brand restaurant in Asia to ship supplies from"`
- تحوّل `Yum!'s` إلى `yums`، وظهور `yum. recognizes` في منتصف الجمل

### السبب الثاني: بناء نص السؤال من بقايا الجملة

الدالة `generate_clean_interrogative_question` كانت تعمل هكذا:

1. تحذف الإجابة من الجملة المصدر
2. تقتطع ما تبقّى إلى ١٢ كلمة
3. تضع الناتج في قالب واحد ثابت:
   `"What is the applicable policy or provision regarding {…}?"`

وحذف عبارة من منتصف الجملة لا يترك خلفه عبارة اسمية سليمة، بل يترك ركاماً لغوياً — وهذا هو
مصدر الأسئلة غير المفهومة تماماً.

### السبب الثالث: اختيار البدائل بشكل عشوائي

كان توليد الخيارات الخاطئة يتم عبر `random.sample()` من كل العبارات الاسمية في الوثيقة، دون
أي تحقق من:

- تطابق النوع الدلالي (كيان جغرافي مقابل كيان زمني)
- كون البديل مرادفاً أو اختصاراً للإجابة
- درجة التشابه الدلالي
- توحيد شكل الحروف الكبيرة والصغيرة

---

## ٣. الحل: الملفات التي أُنشئت وأُعيدت كتابتها

### `text_cleaning.py` — ملف جديد

مسؤول عن تحويل ناتج الـ PDF الخام إلى نثر حقيقي، **مع الحفاظ على علامات الترقيم**.

- `clean_document()`: تحذف فهرس المحتويات، والرؤوس والتذييلات المتكررة، وأرقام الصفحات،
  وتعيد وصل الكلمات المقطوعة بالشرطة، وتُعيد بناء الفقرات من السطور المتقطعة.
- `select_quality_sentences()`: بوابة جودة تشترط في الجملة أن تحتوي على **فاعل وفعل مُصرَّف**،
  وأن يتراوح طولها بين ٨ و٤٥ كلمة، وألّا تبدأ بضمير إشارة غامض، وألّا تكون قد التحمت مع فقرة أخرى.
- `register_brand_tokens()`: تكتشف من الوثيقة نفسها أن `Yum!` وحدة واحدة لا وحدتين — إذ تبيّن
  بالإحصاء أنها ترد في منتصف الجملة بنسبة ٨٥٪ — فتُسجّلها كاستثناء في المُقطِّع، فيتوقف
  انقسام الجمل عندها.

### `candidate_selection.py` — ملف جديد

مسؤول عن اختيار **مفتاح الإجابة**، وهو أساس جودة السؤال كله. يشترط في المرشّح أن يكون:

- **مُرتبطاً بجملة**: يرد حرفياً داخل جملة اجتازت بوابة الجودة
- **ذا مضمون**: تُحذف أدوات التعريف والضمائر، وتُرفض العبارات الفارغة مثل
  `a country` و `this business relationship` عبر قائمة كلمات عديمة الدلالة
- **بارزاً**: يُرتَّب حسب وزن TF-IDF لكلماته
- **غير مكرر الموضوع**: دالة `is_redundant_with()` تمنع أن تدور خمسة أسئلة حول موضوع واحد
  (مثل `Yum! business` و `Yum!'s business` و `Yum! employees`)

كما تُرفض العبارات التي تحتوي على حرف عطف (ناتج خطأ في التحليل النحوي)، أو تنتهي بملكية
معلّقة، أو تحتوي على أقواس غير متوازنة (مثل `Employee(s`).

### `incorrect_answer_generation.py` — أُعيدت كتابته

البديل الجيد يجب أن يقع في نطاق ضيّق: قريب بما يكفي ليكون محتملاً، وبعيد بما يكفي ألّا يكون
إجابة صحيحة ثانية. لذلك طُبّقت المرشّحات التالية بالترتيب:

**أولاً — مرشّحات الشكل الظاهري:**

- رفض المرادف أو الاختصار: `The Us` مقابل `The United States` عبر مقارنة الأحرف الأولى
- رفض اختلاف الإفراد والجمع: `years` مقابل `The Year`
- رفض التداخل: إذا وردت آخر كلمتين من الإجابة داخل البديل
  (`Law Department's approval` مقابل `Yum! Law Department`)
- اشتراط تقارب الطول وعدد الكلمات، وتطابق وجود الأرقام من عدمه

**ثانياً — مرشّح النوع:** إذا كانت الإجابة كياناً مُصنَّفاً (`ORG`، `GPE`، `DATE`...) وجب أن
تكون البدائل من التصنيف نفسه.

**ثالثاً — نطاق التشابه الدلالي:**

```
MIN_SIMILARITY       = 0.30   ← أقل من ذلك: البديل واضح الخطأ ويساعد بالاستبعاد
MAX_SIMILARITY       = 0.78   ← أعلى من ذلك: البديل مرادف، فيصبح للسؤال إجابتان
MAX_MUTUAL_SIMILARITY = 0.82  ← البدائل يجب أن تختلف فيما بينها أيضاً
```

**رابعاً — توحيد شكل الحروف:** الدالة `_match_case()` تُوحّد الشكل دون تشويه. الكود السابق
كان يستخدم `.title()` فيحوّل `Yum!'s Internal Audit Department` إلى
`Yum!'S Internal Audit Department`. الآن تُعدَّل الخيارات المكتوبة بحروف صغيرة فقط.

### `question_extraction.py` — أُعيدت كتابته

مسار **ملء الفراغ**. الفراغ يوضع داخل الجملة الأصلية كاملةً دون تشويه، ويُشترط:

- وجود كلمتين على الأقل قبل الفراغ
- وجود ٦ كلمات على الأقل من السياق حوله

بذلك يستحيل أن ينتج سؤال مثل `"____ brand restaurant in Asia..."` الذي لا يترك للقارئ ما
يستدلّ به.

### `hybrid_assessment_system.py` — أُعيدت كتابته

**القالب القديم حُذف نهائياً.** لم يعد نص السؤال يُبنى من بقايا، بل من إحدى ثلاث صيغ فقط:

1. **استبدال الفاعل** — إذا كانت الإجابة هي الفاعل النحوي كاملاً، يُستبدل بأداة استفهام
   ويبقى باقي الجملة كما هو، فالنتيجة سليمة نحوياً بالضرورة:
   `"New employees will be asked to certify…"` ← `"Who will be asked to certify…?"`

2. **التعريف بالفعل الرابط** — `"X is <answer>"` تصبح `"What is X?"`

3. **ملء الفراغ** — لكل ما عدا ذلك

وأُضيفت ضوابط تمنع الصيغ المعطوبة:

- **مطابقة العدد**: فاعل جمع أمام فعل مُصرَّف يُنتج `"What were resolved…"` وهو خطأ نحوي، فيُرفض
- **العطف**: spaCy يُجزّئ الفاعل المعطوف، فينتج `"What or requirements are…"` — يُرفض
- **الكلام المنقول وضمير المتكلم**: يُنتج
  `"What has asked me to send the supplies … and \"he will take care of it\"?"` — يُرفض
- **النقطتان الرأسيتان**: تُقدّم قائمة فتتحوّل الجملة إلى شظية — يُرفض
- **اختيار أداة الاستفهام**: قائمة أسماء تدلّ على الأشخاص (`employee`، `manager`، `contact`…)
  تجعل الأداة `Who` بدل `What`

أما **الأسئلة المقالية** فتُبنى من جمل الإلزام (`must`، `shall`، `prohibited`…) باستخدام الفاعل
الحقيقي للجملة مع الاحتفاظ بأداة التعريف — إذ كان حذفها ينتج
`"what must franchisee do"` بدل `"what must a franchisee do"`.

### `question_processing.py` — أُعيدت كتابته

كان الملف السابق **يُرقّع** الأسئلة المعطوبة:

- قائمة تعابير نمطية تحذف عبارات سيئة معروفة من نص السؤال
- إكمال الخيارات الناقصة ببدائل مُختلقة مثل `"General company policy"` و
  `"Standard administrative procedure"`

وهذا أسوأ من نقص سؤال واحد، لأن البدائل المُختلقة يسهل تمييزها فيصبح السؤال درجة مجانية.

الملف الآن **يتحقّق ولا يُرقّع**: أي سؤال لا يستوفي الشروط يُحذف ويُعاد ترقيم الباقي. والشروط:
اكتمال النص والإجابة، وجود العدد المطلوب من الخيارات، عدم تكرار أي خيار، ورود الإجابة مرة
واحدة فقط بين الخيارات، وعدم ظهور الإجابة داخل نص السؤال.

### `workers.py` و `question_generation_main.py`

- `question_generation_main.py`: لم يعد يحذف الترقيم، بل يستدعي `clean_document()` ثم يُسجّل
  الرموز التجارية قبل التحليل.
- `workers.py`: يولّد أسئلة زائدة (`n + 4`) ثم يقتطع إلى العدد المطلوب بعد بوابة الجودة، حتى
  لا يؤدي حذف سؤال معطوب إلى نقص في الاختبار.

---

## ٤. النتيجة: قبل وبعد

**مثال ١ — مسار ملء الفراغ**

```
قبل:  brand restaurant in Asia to ship supplies from ____ .
      The United States / The Us / A Country / France
```

```
بعد:  Similarly, employees and directors may not buy or sell securities of
      another company when they have __________ about that company as a
      result of work for or with Yum!.

      material non-public information / sensitive information /
      confidential proprietary information / selective disclosure
```

**مثال ٢ — مسار الأسئلة الاستفهامية**

```
قبل:  What is the applicable policy or provision regarding as an yum.
      recognizes its responsibility to the countries where we do?
      International Business / A Global Company / International Commerce / A Business
```

```
بعد:  Who will be asked to certify their commitment to adhere to the
      standards of conduct contained in this Code?

      New employees / Company personnel / Workplace / Employment
```

**مثال ٣ — السؤال المقالي**

```
قبل:  What must following situations do according to this policy?
      الإجابة المرجعية: نص من غلاف الوثيقة وفهرس المحتويات
```

```
بعد:  What does the Code of Conduct set out in relation to the
      Yum! Brands Social Media Code?

      الإجابة المرجعية: The Yum! Brands Social Media Code describes our required
      rules of engagement in social media - Authenticity, Responsibility and
      Respect - which are meant to protect and safeguard employees…
```

---

## ٥. الاختبارات التي أُجريت

- تشغيل كامل على `pdf/Code-of-Conduct-English_KFC.pdf`: كلا المسارين
  (`txt2questions` و `txt2questions_free`) يُرجعان ٥ أسئلة سليمة.
- فحص الحالات الحدّية: نص فارغ، مسافات فقط، كلمة واحدة، نص بلا أفعال، جملة واحدة صحيحة.
  لا يحدث أي انهيار؛ يُرجع النظام قاموساً فارغاً عند تعذّر التوليد.
- `python -m py_compile` و `pyflakes` على جميع الملفات: لا أخطاء
  (عدا استيرادين غير مستخدمين في `app.py` كانا موجودين من قبل).

---

## ٦. ملاحظات مهمة

### مجلد `models/all-MiniLM-L6-v2`

بما أنك استبعدته لتسريع الاستنساخ، عُدِّل `nlp_models.py` ليبحث عن المجلد المحلي أولاً، فإن لم
يجده حمّل النموذج من HuggingFace تلقائياً. لا حاجة لأي تعديل منك.

### نموذج spaCy

يُفضّل `en_core_web_md`، وعند غيابه يتراجع تلقائياً إلى `en_core_web_sm`. الاختبار جرى على
`sm` لأنه المتوفّر على هذا الجهاز. لتحسين دقّة استخراج الكيانات:

```bash
python -m spacy download en_core_web_md
```

### مكتبة قراءة الـ PDF

مكتبة `PyPDF2` غير مثبّتة على الجهاز، والمثبّت هو `pypdf` (وهي الخلَف المدعوم لها). لذلك
يُجرّب `workers.py` استيراد `pypdf` أولاً ويتراجع إلى `PyPDF2` عند الحاجة.

### مجلد `templates/` مفقود

ملف `app.py` يستدعي `render_template('index.html')` و `quiz.html` و `result.html`، لكن مجلد
`templates/` غير موجود في المستودع. لم أُعدّل `app.py` لأن هذا خارج نطاق تحسين جودة الأسئلة،
لكن **تطبيق Flask لن يعمل حتى تُضاف هذه القوالب**.

---

## ٧. ملخّص الملفات

| الملف | الحالة |
|---|---|
| `text_cleaning.py` | **جديد** — تنظيف الوثيقة وبوابة جودة الجمل |
| `candidate_selection.py` | **جديد** — اختيار مفاتيح الإجابات وترتيبها |
| `question_extraction.py` | أُعيدت كتابته — مسار ملء الفراغ |
| `hybrid_assessment_system.py` | أُعيدت كتابته — الصيغ الاستفهامية والأسئلة المقالية |
| `incorrect_answer_generation.py` | أُعيدت كتابته — توليد البدائل |
| `question_processing.py` | أُعيدت كتابته — التحقّق بدل الترقيع |
| `question_generation_main.py` | عُدِّل — إيقاف حذف الترقيم |
| `workers.py` | عُدِّل — `pypdf` والتوليد الزائد |
| `nlp_models.py` | عُدِّل — تحميل النماذج مع بدائل احتياطية |
| `app.py` | دون تغيير |
